{{form_start(form, {'attr': {'class':'d-flex', 'action':path('op_webapp_public_dashboard_search'),}})}}
{# Bannière sur la vignette du bien #}
<div class="input-group input-group-sm">
    {{ form_widget(form.words) }}
    <button type="submit" id="btnSearchPropertyHome" class="btn btn-secondary" type="button"><i class="fa-light fa-magnifying-glass-location"></i></button>

</div>
{{form_end(form)}}