{% extends 'admin.html.twig' %}

{% block title %}Edit productFormat{% endblock %}

{% block content %}
<div class="container-fluid">
    {{ include('gestapp/product_format/include/_section_content_header.html.twig') }}
    <section id="op_admin_content_body">
        <div class="row g-2">
            <div class="col-2">
                {{ include('gestapp/product_format/include/_section_content_aside.html.twig') }}
            </div>
            <div class="col-10">
                <div class="card">
                    <div class="card-header">
                        <h3>Modifier un Format</h3>
                    </div>
                    {{ include('gestapp/product_format/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                </div>
            </div>
        </div>
    </section>
</div>

{% endblock %}
