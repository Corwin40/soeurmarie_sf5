<div class="card">
    <div class="card-body">
        <h4>
            {% if app.request.get('_route') == 'op_gestapp_product_index' %}
                <a href="{{ path('op_admin_purchases_index') }}"><i class="fas fa-chevron-circle-left"></i> Retour</a>
            {% else %}
                <a href="{{ path('op_admin_purchases_index') }}"><i class="fas fa-chevron-circle-left"></i> Retour</a>
            {% endif %}
        </h4>
    </div>
</div>
